services:
  postgres:
    image: postgres:16
    container_name: scholarship-autofill-postgres
    environment:
      POSTGRES_DB: scholarship_autofill
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
